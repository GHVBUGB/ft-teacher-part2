[project]
name = "ft-training-common-api"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["fastapi", "pydantic>=2", "sqlalchemy>=2", "alembic", "psycopg[binary]>=3", "uvicorn", "httpx"]
[dependency-groups]
dev = ["httpx", "pytest"]
