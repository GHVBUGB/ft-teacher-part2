# FT 词句清单接入 · 2026-09-14

来源：用户提供的 `FT Target Words.xlsx`，Sheet1!A1:D156。原 Excel 未修改。

已接入 **104 个单词、129 条句子朗读内容**。其中 3 条朗读内容各包含两句话，合计 132 个完整句子。题目按原表首次出现顺序排列，重复内容只练一次。

## 内容范围

| 原表列 | 原始非空记录 | 处理 |
| --- | ---: | --- |
| Target Word（B） | 117 | 展开明确的单复数选项、合并重复；排除 2 个多词短语，得到 104 个单词 |
| Target Phrase（C） | 59 | 用户确认本轮暂不接入 |
| Target Sentence（D） | 110 | 展开明确的斜杠选项、合并重复；暂缓 4 条未完成句型及 2 条待确认表达，得到 129 条朗读内容 |

`board game`（B51）和 `bell peppers`（B101）虽然写在单词列，但属于多词短语，与短语一并暂缓；现有单词评分接口仅接受一个单词，没有为了兼容它们变更接口或评分规则。

未填写的中文解释、音标、提示与例句保持为空，不把自行生成内容标作业务提供资料。语法仍是原来的 3 个示例知识点。

## 整理与待确认内容

明显错误仅在应用题库中修正，原文保存在来源记录中：

| 单元格 | 原文 | 应用内容 |
| --- | --- | --- |
| B124 | achipelago | archipelago |
| D42 | They are bird. | They are birds. |
| D141 | Let's try bumber cars. | Let's try bumper cars. |

以下内容暂不作为标准朗读题：

| 单元格 | 原文 | 待确认事项 |
| --- | --- | --- |
| D80 | She's wearing a coat/pants and boots. | 斜杠的替换范围 |
| D100 | I'd like some/I prefer some burger. | burger 的数量及完整表达 |
| D113 | My/His/Her hobby is ……….. | 补全句子 |
| D114 | I/He/She think(s) it's …….. | 补全句子 |
| D115 | It can make/help ……. | 补全句子 |
| D119 | We should/shouldn't……. | 补全句子 |

D144 的单词 green 与句子 It's red. 不对应；句子栏原文保留并合并到已有同句，不自行生成 It's green.。D60–D62 各两句话仍作为一条完整朗读内容。仅规范多余空格和末尾标点。

## 接口与合并

- 沿用 `catalog.ts` 的 `words` 数组与 `id/text/ipa/zh/kind/hint/example` 字段，`stageCatalog.ts` 自动映射，没有新增 API、数据库字段或运行时导入入口。
- `CONTENT_VERSION` 更新为 `ft-target-words-2026-09-14-v1`，使用既有按内容版本隔离机制；新题使用稳定标识，旧样例分数不会成为新题通过记录。旧版语法示例记录也会随既有内容版本校验失效，语法题本身不变。
- 单词总分、句子总分公式、逐题至少 80 分、全部通过才解锁以及保存结构均不变。
- 题目列表仅调整为限高可滚动网格，便于访问 104/129 个题目，不添加新筛选字段或进度功能。
- 233 条示范音频由本机 macOS Samantha（en_US）合成，使用现有 `/samples/{id}.wav` 路径；16kHz、单声道、16-bit PCM。它们是试听资料，不是培训师真人标准录音或发音准确性验证。

## 维护与追溯

`ft-target-words-2026-09-14.json` 记录每条题目对应的源单元格、课程、原文、展开、纠错及暂缓原因；完整源单元格均可对账。它是离线交接资料，不作为新增前端数据接收入口。

`scripts/import-ft-targets.py` 针对此次文件哈希与明确单元格映射生成原有题库数组；文件变化时需审核映射后更新版本，不自动套用旧替换规则。

`scripts/generate-ft-samples.py` 根据题库来源清单生成试听 WAV；`ft-target-audio-2026-09-14.json` 记录逐题时长、格式和文件哈希。

验收记录与本次真实截图位于 `artifacts/testing/2026-09-14-target-content/`。关卡边界测试使用隔离的合成成绩，未调用真实评分供应商。
