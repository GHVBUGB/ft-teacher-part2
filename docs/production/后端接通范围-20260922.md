# 完整后端接通范围

授权：用户2026-09-22明确要求完整后端，并要求先申请SpeechSuper延期。

## 当前事实
- 已发布Sites静态网站；没有可运行的云端Python API。
- 登录固定演示验证码、身份及进度为mock；前端仍持有语法答案。
- Neon已有题库、规则、账号、会话、验证码、考核轮次、提交评分和时间统计表，建表不等于接库。
- server/production/grammar.py只有纯规则核心，尚未绑定会话、事务和路由。

## 必须完成的实际链路
- 白名单邮箱验证码，或用户提供的独立模拟账号密码；真实会话Cookie和退出。
- 从认证会话确定教师；所有轮次、分配、成绩、反馈做所属人校验。
- 题目顺序、进度、每次提交、服务端评分及耗时存入数据库；刷新及跨设备恢复。
- 语法正确答案仅服务端持有；按现有30题24对及第7错提前结束规则执行。
- 词句录音经受保护的后端送SpeechSuper；真实分数回库，幂等防重复，失败不算答错。
- 供应商免费有效期和并发限额由服务端约束；不自动转付费。
- Sites当前静态模式必须改成可运行后端的部署形式，或独立云API；需核实免费运行时和PostgreSQL连通能力。

## 前置外部项
- SpeechSuper延期申请等待用户审核及供应商确认。
- 没有已验证的邮件发送服务；实际测试账号名单未交付。本次不伪造邮箱验证或公司教师身份。
- Render账号触发银行卡验证，已取消；不使用付费套餐绕过。

## 验收证据
至少两独立账号：登录、跨账号拒绝、三关提交、重复请求、刷新恢复、24/30及第7错、真实词句评分、数据库独立回读、退出后拒绝访问。

状态：范围核对完成，完整后端尚未实现或上线。


## 本轮落地与验证（2026-09-22）
- SpeechSuper延期申请已按用户批准正文发送，Gmail已发回读唯一1封；供应商未确认延期。
- 新增 production.app 实际后端：密码/白名单验证码、服务端会话、教师所属人校验、三关轮次、服务端判分、成绩/时长/反馈保存。
- 009迁移新增限流及供应商并发租约；尚未应用到Neon正式库。
- 语音请求提交先持久化，再调用；重复请求不再次调用。进程中断留下pending需核查，不能编造分数或静默重扣。
- 新增独立真实接口验收前端；FT_FRONTEND_MODE=live 构建，原默认演示入口不变。
- 独立PostgreSQL 16隔离库执行001—009，并通过17项单元/集成测试；SMTP和SpeechSuper为测试替身，不是供应商实测。
- 真实前端typecheck/build通过；包内无246810、Demo Teacher和语法题库；原10组规则回归通过；静态路由/API隔离4项通过。
- 还没有浏览器端完整体验验收、真实邮件验证码投递、供应商真实评分、云端接库及云部署。
- 2026-09-22实时打开Neon项目Functions页，当前支持Node.js函数（自动注入DATABASE_URL）；现有Python入口须转换/另选兼容运行时，不能直接发布到Sites静态站。
- Neon Functions官方Free计划有免费额度，资料：https://neon.com/blog/neon-backend-is-ga 。未开通任何收费方案。

结论：申请已发送；本地真实后端及接口验收入口已实现并完成部分测试；公网仍是原演示版。
