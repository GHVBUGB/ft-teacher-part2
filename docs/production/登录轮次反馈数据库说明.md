# 登录、朗读轮次、反馈扩展

2026-09-20：迁移003已在本项目Neon数据库提交，保留已有题库与数据。

新增4表：login_challenges（验证码摘要/有效期/失败次数/使用时间）、speech_rounds（归属/版本/关卡/状态/时间）、speech_round_items（本轮题目/顺序）、teacher_feedback（归属/页面/题目上下文/内容/处理状态/提交去重）。
扩展teachers：is_test_account、可空password_hash。
扩展speech_assessments：round_id、scoring_rule_version、pronunciation、fluency、rhythm、integrity。

## 验证

数据库内运行 server/production/tests/003_constraints.sql，12项约束检查通过，事务最终ROLLBACK。模拟账号没有正式创建。
测试涵盖验证码次数/时间、轮次教师归属、重复进行中轮次、题型、完成时间、成绩有效写入/重复提交/维度范围、反馈归属/完成时间。
证据：项目上级 artifacts/testing/2026-09-20-cloud/schema-003-tests.txt、schema-003-tests.png。

## 接入时必须执行的逻辑（尚未实现）

- 验证码：使用服务端秘密密钥HMAC，不使用可离线穷举的裸短码哈希；原子消费未使用且未过期的挑战；校验启用白名单、限制发码和尝试频率。当前字段约束不能替代在线认证。
- 密码：使用专门的密码哈希算法并校验，不是简单长度校验；本次没有设置任何密码。
- 朗读：服务端事务抽满30词/20句，冻结顺序；新提交必须关联轮次（可空仅兼容历史）；按当前登录身份鉴权，禁止读取他人数据。
- 评分：验证句子四个维度齐全，按绑定版本计算，不能信任前端传入总分/通过状态；保存评分规则版本。
- 反馈：服务端验证题目存在、归属及处理权限；写入updated_at与resolved_at；page_path不含令牌和个人敏感查询参数。
- 全部表结构完成不代表登录、跨设备恢复、反馈页面或前后端接库已实现。数据库外键约束不等于接口访问权限隔离。
